[project]
name = "loop-os"
version = "0.1.0"
description = "Loop OS — kernel/OS/application research system over the experiment loop"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6.0"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["os", "."]
