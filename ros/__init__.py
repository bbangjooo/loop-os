"""Research OS 2 — the OS layer over the experiment-loop kernel.

Instruments (each a pure function: files in → files out + at most one
journal append): journal, aim, seal. See docs/design.md.
"""

__version__ = "0.1.0"
